# CEVAE 项目代码变更总结

## 变更日期
2025-03-16

## 变更概览

本次更新主要包含三个方面：
1. ✓ GPU 加速配置
2. ✓ 模型保存路径优化（支持多数据集/多重复实验独立保存）
3. ✓ 测试脚本和文档完善

---

## 详细变更

### 1. GPU 优化 (cevae_ihdp.py)

#### 新增导入
```python
import os  # 第12行
```

#### GPU 配置代码 (第39-58行)
```python
# ============================================================================
# GPU Configuration
# ============================================================================
config = tf.ConfigProto()
config.gpu_options.allow_growth = True
config.gpu_options.per_process_gpu_memory_fraction = 0.9
config.allow_soft_placement = True
config.log_device_placement = False

# Print GPU information
print('=' * 60)
print('GPU Configuration:')
print('- Allow growth: Enabled')
print('- Memory fraction: 90%')
print('- Soft placement: Enabled')
print('=' * 60)
# ============================================================================
```

#### TensorFlow Session 修改
```python
# 第108行，使用GPU配置
sess = tf.InteractiveSession(config=config)
```

### 2. 模型保存路径优化

#### 创建 models 目录 (第81-85行)
```python
# Model save path based on dataset
# Create models directory if it doesn't exist
if not os.path.exists('models'):
    os.makedirs('models')
    print('Created models/ directory for saving trained models')

# Base model path - will be extended with replication index if needed
model_base_path = 'models/cevae_{}'.format(args.dataset)
```

#### 循环内动态计算模型路径 (第98-111行)
```python
for i, (train, valid, test, contfeats, binfeats) in enumerate(dataset.get_train_valid_test()):
    print('\nReplication {}/{}'.format(i + 1, num_replications))

    # Determine model path for this replication
    # For multi-replication datasets, save each replication separately
    if num_replications > 1:
        model_path = '{}_rep{:03d}'.format(model_base_path, i + 1)
        print('Model will be saved to: {}'.format(model_path))
    else:
        model_path = model_base_path
        print('Model will be saved to: {}'.format(model_path))

    (xtr, ttr, ytr), (y_cftr, mu0tr, mu1tr) = train
    # ...
```

### 3. 新增文件

#### tests/test_all_datasets.py
完整的数据集测试脚本，用于验证：
- IHDP 数据集加载 (10个重复实验)
- TWINS 数据集加载
- IHDP1000 数据集加载（如果可用）

#### docs/训练说明文档.md
完整的训练指南，包含：
- 更新内容说明
- 环境要求
- 配置步骤
- 运行命令
- 输出文件说明

---

## 模型保存路径对照表

| 运行命令 | 模型保存路径 |
|---------|-------------|
| `python cevae_ihdp.py -dataset ihdp` | `models/cevae_ihdp_rep001` ~ `models/cevae_ihdp_rep010` |
| `python cevae_ihdp.py -dataset ihdp1000` | `models/cevae_ihdp1000` |
| `python cevae_ihdp.py -dataset ihdp1000 -separate_reps` | `models/cevae_ihdp1000_rep001` ~ ... |
| `python cevae_ihdp.py -dataset twins` | `models/cevae_twins` |

---

## 环境配置提醒

**重要**: 由于项目使用 TensorFlow 1.1.0，需要 Python 3.5 环境。

当前检测到的 Python 版本是 3.14.2，**不兼容**本项目。

请在 Windows CMD/PowerShell 中运行：
```cmd
tests\setup_env.bat
```

---

## 文件清单

### 修改的文件
- `cevae_ihdp.py` - 添加GPU配置和模型路径优化

### 新增的文件
- `tests/test_all_datasets.py` - 数据集测试脚本
- `docs/训练说明文档.md` - 完整使用指南
- `docs/代码变更总结.md` - 本文档

### 需要的文件（环境配置）
- `tests/setup_env.bat` - 已存在，环境配置脚本
- `tests/verify_env.py` - 已存在，环境验证脚本

---

## 验证步骤

环境配置完成后，按以下顺序验证：

1. **激活环境**
   ```cmd
   activate cevae
   ```

2. **验证依赖**
   ```cmd
   python tests\verify_env.py
   ```

3. **测试数据集**
   ```cmd
   python tests\test_all_datasets.py
   ```

4. **运行训练**
   ```cmd
   python cevae_ihdp.py -dataset ihdp -reps 10 -epochs 100
   ```

---

## 已知问题

1. **IHDP1000 数据集未安装**
   - 当前 `datasets/IHDP1000/` 目录不存在
   - 该数据集需要单独下载
   - IHDP 和 TWINS 数据集已可用

2. **GPU 驱动**
   - 如果系统没有 NVIDIA GPU，代码会自动回退到 CPU
   - 训练速度会较慢

---

*变更完成日期: 2025-03-16*
*项目: B:\Experiments\CEVAE*
